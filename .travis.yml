language: node_js
sudo: required

services:
  - docker

before_script:
  - docker-compose build
  - docker-compose up
  - sleep 10

script:
  - docker exec web /bin/sh -c "cd /usr/src/app && npm run test"
  - docker-compose run sut test

branches:
  only:
    - master
